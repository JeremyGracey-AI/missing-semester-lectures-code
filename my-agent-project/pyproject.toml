[project]
name = "my-agent-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nooa>=0.0.8",
]

[tool.uv.sources]
nooa = { git = "https://github.com/NVIDIA-NeMo/labs-OO-Agents.git", rev = "main" }
